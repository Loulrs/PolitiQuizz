CREATE TABLE questions (
    id SERIAL PRIMARY KEY,
    question_text TEXT NOT NULL,
    points_yes INT NOT NULL, -- Points si réponse "Oui"
    points_no INT NOT NULL   -- Points si réponse "Non"
);
